<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="student.if319028.uashappy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_turbo2"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_turbo2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CRUD.Ubah" android:parentActivityName=".APIManageSoal"></activity>
        <activity
            android:name=".CRUD.Tambah"
            android:parentActivityName=".APIManageSoal" />
        <activity android:name=".About" />
        <activity android:name=".APIManageSoal" />
        <activity android:name=".APIReadSoal" />
        <activity android:name=".Profile" android:parentActivityName=".Welcome"/>
        <activity android:name=".Welcome" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>